// The generated `api.ts` exports the request/response zod schemas (PascalCase,
// e.g. `LoginRequest`, `CreateEnrollmentRequest`) used for server-side
// validation. The `types/` folder re-exports plain TS interfaces of the same
// names, so we only surface the zod schemas here to avoid name collisions.
export * from "./generated/api";
